# مشروع تفريغ دروس تفسير الطبري - الشيخ حسين

هذا الملف يحتوي على السياق والقواعد الأساسية للمشروع لكي يعتمد عليها المساعد الذكي (AI) في الجلسات القادمة.

## 1. معلومات المشروع
- **الهدف:** تحويل تفريغات نصية لدروس "تفسير الطبري" إلى كتاب إلكتروني احترافي ومفهرس.
- **المحرك المستخدم في LaTeX:** محرك **XeLaTeX** (لأنه يدعم اللغة العربية وحزمة `polyglossia`).
- **هيكل المشروع:**
  - `main.tex`: الملف الأساسي (يحتوي على إعدادات الغلاف، الفهرس، الحزم، ويستدعي الدروس).
  - مجلد `src/`: يحتوي على ملفات الدروس المستقلة (مثل `001.tex`، `002.tex`).

## 2. الأوسمة والبيئات المعتمدة (Macros & Environments)
يجب استخدام الأوسمة التالية عند تنسيق أي تفريغ جديد:

- **للآيات القرآنية** (مع ضبطها بالتشكيل التام):
  `\begin{ayah} النص القرآني \end{ayah}`
- **للأحاديث النبوية ومتونها**:
  `\begin{hadith} نص الحديث \end{hadith}`
- **لأفعال الأداء في الإسناد** (حدثنا، عن، قال):
  `\isnad{حدثنا}`
- **لأسماء الرواة والأعلام**:
  `\rawi{اسم الراوي}`
- **للفواصل الزخرفية بين الفقرات**:
  `\separator`
- **لإبراز الفوائد المستنبطة من الدرس**:
  `\begin{fawaid}[عنوان اختياري] نص الفائدة \end{fawaid}`

## 3. التنسيق العام والصياغة
- تصحيح أخطاء التفريغ الصوتي التلقائي وحذف لزمات الكلام الشفوية لتناسب أسلوب الكتب.
- استخدام `\chapter{}` لعنوان الدرس الرئيسي.
- استخدام `\section{}` و `\subsection{}` للتقسيمات الداخلية ليتم إدراجها تلقائياً في الفهرس `\tableofcontents`.
- استخدام القوائم `\begin{itemize}` للنقاط و `\begin{enumerate}` للترقيم.
- وضع أسماء الكتب والمصنفات التراثية بين أقواس اقتباس عربية `«اسم الكتاب»`.

## 4. مسار العمل (Workflow) للدروس الجديدة
1. يقوم المستخدم بتزويد الذكاء الاصطناعي بنص التفريغ (مثلاً `002.txt`).
2. يقوم الذكاء الاصطناعي بتنسيقه على أجزاء وتوليد كود LaTeX لملف `src/002.tex`.
3. يتم تفعيل استدعاء الدرس في الفهرس الرئيسي `main.tex` عبر `\input{src/002.tex}`.
4. يتم تصنيف أي فوائد تُستخرج من الدرس في صندوق `\begin{fawaid}` داخل الدرس نفسه، وتُرحَّل كخلاصة أو قائمة نقاط في المكان المناسب من ملحق الفوائد `src/appendices.tex`.