# كيف يُحرَّر هذا الكتاب — AUTHORING

> **الأصلُ LaTeX، وملفُّ Word نسخةٌ مُصدَّرةٌ منه لا مصدرٌ له.**
> فمَن حرَّر الـdocx حرَّر ورقةً تُطبَع، لا كتابًا يُبنى.

## القاعدة

| | |
|---|---|
| الأصلُ المعتمَد | `main.tex` ومعه ما يُدخِله |
| البناء | `latexmk -xelatex -interaction=nonstopmode main` |
| تصديرُ docx | لا أداةَ مخصَّصة — pandoc على الملفّ المجموع |
| ملفُّ docx | **مُخرَجٌ** — و`*.docx` غيرُ متابَعٍ في git (انظر `.gitignore`) |

## لِمَ لا يُحرَّر الـdocx

ثلاثةُ أشياء تسقط في التصدير، ولا تُستردّ في العكس:

1. **أوامرُ الكتاب الخاصّة.** أوامرُ المعنى (التخريج، الآيات، الأحاديث، الأحكام،
   والبيئاتُ المعرَّفةُ في التمهيد) لا يعرفها pandoc، فتُبسَط إلى نصٍّ صريح. وما
   بُسِط لا يُرَدُّ إلى أصله: عليها تقوم الفهارسُ وأدواتُ الفحص.
2. **الاتّجاه.** ملفُّ docx يأخذ اتّجاهَه من أنماط المستند لا من سطر الأمر، فبغير
   مرجعٍ مُعَدٍّ يخرج النصُّ من اليسار وتنقلب علاماتُ الترقيم.
3. **أرقامُ الحواشي.** تُعاد من أوّلها إن صُدِّر كلُّ فصلٍ على حدة.

وأبعدُ من ذلك: البناءُ يُعاد فيَطمس ما كُتب في `build/`، والـPDF يُبنى من LaTeX
لا من Word — فتعديلُ الـdocx لا يبلغ الكتابَ أصلًا، ويبقى الاثنان يفترقان صامتين.

## الدورةُ المعتمَدة

1. **يُصدَّر** docx طازجٌ من LaTeX (أو يُؤخذ آخرُ تصديرٍ في `build/`).
2. **تُنسَخ** نسخةٌ للتعليم — لا يُعلَّم على ما في `build/`، فهو يُطمَس في البناء.
3. **يُعلِّم المراجعُ** في OnlyOffice: **Collaboration ▸ Track Changes** مُشغَّلًا،
   والتعليقُ بـ **Collaboration ▸ Comment**. ويُحفَظ `.docx` لا ODF.
4. **تُستخرَج الملاحظاتُ** إلى md مُتابَع:
   ```
   python3 ~/work/helghareeb-hub/tools/docx_review.py <الملفّ.docx> -o "ملاحظات-المراجعة/<الجولة>/ملاحظات.md"
   ```
5. **تُنقَل إلى LaTeX** في `main.tex` وما يُدخِله — بالعين، بندًا بندًا.
6. **تُشغَّل أدواتُ الفحص** في `tools/` إن كانت.
7. **يُعاد البناء**، ويُصدَّر docx جديدٌ متى طُلب للمصمِّم أو للناشر.
8. **يُكتَب الكوميت**: تعديلُ LaTeX + ملفُّ الملاحظات المستخرَج. والـdocx لا يُتابَع.

فالثنائيُّ لا يُتابَع، وناتجُه النصيُّ يُتابَع — وهي قاعدةُ المستودع نفسِه في
`.gitignore`، لا قاعدةٌ جديدة.

## تنبيه

إن خرج ملفُّ الملاحظات فارغًا فلعلّ «تتبُّعَ التغييرات» كان مُطفأً حين التعليم:
يُحفَظ التعديلُ نصًّا عاديًّا لا يُميَّزُ عن الأصل، فلا يراه المستخرِج. يُراجَع
الإعدادُ ويُعادُ التعليمُ — والتعليقاتُ تُقرأ وإن كان التتبُّعُ مُطفأً.
